spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://127.0.0.1:15432/tourn3}
    username: ${DB_USERNAME:tourn3}
    password: ${DB_PASSWORD:tourn3}

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        format_sql: true

#logging:
#  level:
#    root: DEBUG
#    org.springframework: DEBUG
#    org.hibernate.SQL: DEBUG
#    org.hibernate.type.descriptor.sql: TRACE  # Hiển thị giá trị của parameters trong câu SQL
